# 智能足浴机器人三端业务逻辑流程图

> 依据当前桶体工程 `Foot_bath_robot_V1`、基站工程 `Foot_bath_robot_base_V2` 代码整理。  
> 本文只描述当前已经实现的逻辑；Linux 主控的 APP、底盘运动和任务调度细节未包含在两个 MCU 工程中，因此在总流程图中以“产品层决策”表示。

## 一、Linux 主控、桶体、基站：开机到洗脚结束

## 阅读方法

- 圆角框表示开始或结束，矩形表示执行动作，菱形表示条件判断。
- 箭头上的 A2、B2、0x07 表示触发路径的命令或状态。
- 回环表示 MCU 周期任务持续检查条件，并不表示程序卡住。
- 第一次阅读先看“上电 → 注水 → 足浴 → 结束处理 → 待机”主线，再看故障分支。

### 这张图讲什么

第一张图按 Linux 主控、桶体 MCU、基站 MCU 三条泳道展开。主业务链是：

**三端上电 → 建立通信 → 可选自检 → APP 设置参数 → B2 自动注水 → A 类命令启动足浴 → A7 或定时结束 → B4 排水或 B3 自动清洁 → 基站回到 0x03 待机。**

- **Linux 主控是决策者**：接收 APP 操作、组织命令、判断能否进入下一阶段。
- **桶体是通信中转和足浴执行端**：离站到达指定点位后执行恒温、按摩、UV 和水位检测，并透传 B 类命令。
- **基站是水路和清洁执行端**：桶体对接基站时负责进水、排水、喷淋、药液、加热和烘干。
- 桶体将自身数据和基站数据合并后，每秒上传 Linux。

```mermaid
flowchart TD
    START([三端上电])

    subgraph LINUX[Linux 主控与 APP]
        L1["启动 Linux 服务<br/>初始化 APP 通信与底盘控制"]
        L2["周期发送 30 字节协议帧<br/>链路模式 data[2]=0x02 时允许桶体透传基站"]
        L3{"1 秒合并状态帧是否正常?"}
        L4["显示桶体 data[0..15]<br/>显示基站 data[16..28]"]
        L5{"是否执行开机自检?"}
        L6["发送 A8：桶体自检<br/>桶体立即回包，约 5 秒完成后再次回包"]
        L7["发送 B8：基站自检<br/>检查执行器与喷淋电机位置"]
        L8{"是否满足服务条件?<br/>无关键故障、桶体/基站在线、液位满足"}
        L9["APP 获取足浴参数<br/>目标水位 data[16]<br/>目标温度 data[6]<br/>药液泵时长 data[20]/data[21]"]
        L10["必要时控制底盘对接基站<br/>此部分属于 Linux 产品层"]
        L11["发送 B2 自动注水"]
        L12{"基站状态是否到达<br/>0x05 保温或 0x06 待排水?"}
        L23["确认基站已停止进水和相关水路输出"]
        L24["Linux 控制底盘携桶体离开基站<br/>前往用户指定足浴点位"]
        L25{"是否已离开基站并到达指定点位?"}
        L13["到点后才发送桶体功能命令<br/>A2 恒温、A4 按摩、可选 A5 UV<br/>A6 只设置 data[9] 定时"]
        L14["离站点位执行足浴<br/>APP 展示水温、水位、剩余时间、错误码"]
        L15{"结束条件?"}
        L16["定时到期：桶体自动关闭<br/>恒温、按摩、泵阀、UV并待机"]
        L17["用户主动结束：发送 A7<br/>关闭桶体全部输出并清除定时"]
        L26["Linux 控制底盘携桶体返回基站"]
        L27{"桶体是否已物理对接基站<br/>且桶体-基站通信在线?"}
        L28["禁止排水和清洁<br/>提示重新对接基站"]
        L18{"连接确认后选择结束处理方式"}
        L19["发送 B4 强制排水"]
        L20["发送 B3 自动清洁"]
        L21{"基站返回待机 0x03?"}
        L22["记录本次服务结果<br/>保持周期通信或进入低功耗"]
        LERR["APP 提示故障并禁止启动<br/>按故障类型执行重试、补液或检修"]
    end

    subgraph BUCKET[桶体 MCU]
        T1["初始化传感器与执行器<br/>输出默认关闭，主状态 POWER_ON=0"]
        T2["启动 Linux UART、基站 UART DMA<br/>启动周期任务"]
        T3["接收 Linux 帧并校验帧头/校验和"]
        T4["A0..A9：本地执行<br/>B0..BF：透传基站<br/>0x00：写入实时水位 data[5]、温度 data[6] 后透传"]
        T5["每 1 秒生成唯一常规 Linux 上报<br/>data[0..15] 桶体实时状态"]
        T6{"基站 3 秒内在线?"}
        T7["合并最近基站 data[16..28]"]
        T8["基站区域 data[16..28] 保持 0"]
        T9["自检期间忽略除 A8 外的桶体控制命令"]
        T10["执行恒温、按摩、UV、定时<br/>实时检查缺水、超温、过流、电池异常"]
        T11["收到基站状态 data[23]<br/>0x07/0x09 开内循环<br/>0x08/0x0A/0x0C 开排水<br/>其余状态按本地功能安全关闭/保持"]
        T12{"Linux 有效帧是否超过 5 秒未到?"}
        T13["通信超时：关闭全部桶体输出<br/>清除定时，进入待机"]
    end

    subgraph BASE[基站 MCU]
        B1["初始化上水、加热、喷淋、药液泵<br/>风机、排水和位置电机，输出默认关闭"]
        B2["接收桶体透传帧<br/>仅处理有效 data[2]=0x02 帧"]
        B3["B0..BF 进入命令状态机<br/>0x00 更新桶体实时水位/温度"]
        B4["B2：目标水位=data[16]<br/>目标温度=data[6]<br/>打开进水并按需加热/加药"]
        B5{"已收到有效实时数据且<br/>当前水位 >= 目标水位?"}
        B6["关闭进水和基站加热<br/>进入 0x05 或 0x06"]
        B7["B4：喷淋电机到排水位 0 度<br/>基站排水阀打开，状态 0x0C"]
        B8["B3 自动清洁：<br/>排残水→清洁喷淋→排水→清水喷淋→排水→热风烘干"]
        B9["每个排水阶段：水位到 0L 后继续 100 秒<br/>未到 0L 最长 10 分钟"]
        B10["动作完成，喷淋电机回排水位<br/>全部输出关闭，状态 STANDBY=0x03"]
        B11{"桶体有效帧是否超过 5 秒未到?"}
        B12["桶体离线：若当前动作依赖桶体<br/>停止动作并回待机"]
        B13["每个有效桶体帧立即回复状态<br/>无新帧时仍每 1 秒主动回复"]
    end

    START --> L1
    START --> T1 --> T2
    START --> B1
    L1 --> L2 --> T3 --> T4 --> B2 --> B3
    T2 --> T5 --> T6
    T6 -- 是 --> T7 --> L3
    T6 -- 否 --> T8 --> L3
    B3 --> B13 --> T5
    L3 -- 否 --> LERR
    L3 -- 是 --> L4 --> L5
    L5 -- 是 --> L6 --> T9 --> L7 --> L8
    L5 -- 否 --> L8
    L8 -- 否 --> LERR
    L8 -- 是 --> L9 --> L10 --> L11
    L11 --> T3
    B3 -->|B2| B4 --> B5
    B5 -- 否 --> B4
    B5 -- 是 --> B6 --> B13
    B6 --> L12
    L12 -- 否，继续接收 1 秒状态 --> L12
    L12 -- 是 --> L23 --> L24 --> L25
    L25 -- 否，继续导航或重新定位 --> L24
    L25 -- 是 --> L13 --> T3
    T4 -->|A2/A4/A5/A6| T10 --> T11
    T10 --> T12
    T12 -- 是 --> T13 --> T5
    T12 -- 否 --> T10
    L13 --> L14 --> L15
    L15 -- 定时到期 --> L16 --> L26
    L15 -- 用户停止 --> L17 --> L26
    L15 -- 尚未结束 --> L14
    L26 --> L27
    L27 -- 否 --> L28 --> L26
    L27 -- 是 --> L18
    L18 -- 只排水 --> L19 --> T3
    L18 -- 自动清洁 --> L20 --> T3
    B3 -->|B4| B7 --> B9 --> B10
    B3 -->|B3| B8 --> B9 --> B10
    B10 --> B13 --> L21
    L21 -- 否 --> L21
    L21 -- 是 --> L22
    B2 --> B11
    B11 -- 是 --> B12 --> B10
    B11 -- 否 --> B2
```
### 正常业务示例

假设目标水位 8L、目标温度 42℃、足浴 20 分钟：

1. Linux 持续发送有效帧，桶体以透传模式连接基站。
2. Linux 下发 B2：`data[16]=8`、`data[6]=42`；桶体将其透传给基站。
3. 基站打开进水和按需加热；Linux 继续发送 `0x00` 摸鱼帧。
4. 桶体把实际水位写入 `data[5]`、实际温度写入 `data[6]`，再转给基站。
5. 水位达到 8L 后，基站关闭进水和加热，进入 `0x05` 或 `0x06`。
6. Linux 确认注水完成，再发送 A2、A4、A6。
7. 基站关闭进水后，Linux 控制底盘携桶体离开基站并前往指定点位。
8. Linux 确认已离站并到点后，才发送 A2、A4、A6；桶体开始恒温、按摩和倒计时。
9. 足浴结束后桶体关闭输出，Linux 控制底盘返回并重新对接基站。
10. 只有确认物理对接且桶体-基站通信在线后，Linux 才能发送 B4 排水或 B3 自动清洁。
11. 基站回到 `data[23]=0x03` 后，本次服务完成。

### 三条异常主线

- **Linux 断联**：桶体 5 秒无有效 Linux 帧，关闭输出并待机。
- **桶体看不到基站**：3 秒无基站状态，`data[16..28]` 清零。
- **基站看不到桶体**：5 秒无桶体有效帧，依赖桶体的动作会被中止。



### 总流程关键约束

- Linux 到桶体使用固定 30 字节帧；`data[2]=0x02` 才是当前三端透传模式。
- 桶体常规上报 Linux 每 1 秒一帧；桶体自检开始和完成保留即时单独回包。
- 基站在线时，桶体将最近基站 `data[16..28]` 合并到周期帧；基站超过 3 秒无状态帧时，该区域清零。
- Linux 超过 5 秒没有给桶体有效帧，桶体关闭所有输出；基站超过 5 秒没有收到桶体有效帧，会中止依赖桶体的动作。
- B2 是否完成只使用实时摸鱼帧中的 `data[5]` 水位判断；目标水位只取 B2 的 `data[16]`。

## 二、桶体业务逻辑流程图

### 这张图讲什么

第二张图是桶体周期任务形成的循环，不是一次走完的线性流程：

1. **收命令**：区分 A 类桶体命令、B 类基站命令、`0x00` 摸鱼命令和系统命令。
2. **控外设**：控制恒温、按摩电机、UV 和泵阀。
3. **做保护**：检查缺水、传感器、超温、加热超时、过流和电池异常。
4. **发状态**：每秒生成桶体与基站合并状态发给 Linux。

阅读时先从 `data[3]` 命令类型找到对应命令，再看动作回到周期循环后受到哪些保护条件约束。

```mermaid
flowchart TD
    T0([桶体上电]) --> T1["初始化 GPIO、ADC、DMA、UART、传感器<br/>加热/按摩/泵阀/UV全部关闭"]
    T1 --> T2["主状态 POWER_ON=0<br/>错误码清零，默认最近命令 A1"]
    T2 --> LOOP{"周期任务循环"}

    LOOP --> RX{"收到完整 30 字节帧?"}
    RX -- 否 --> SAFETY
    RX -- 是 --> VALID{"帧头与校验和有效?"}
    VALID -- 否 --> SAFETY
    VALID -- 是，来自 Linux --> CMD{"data[3] 命令类型"}
    VALID -- 是，来自基站 --> BASEFRAME

    CMD -- 0x00 摸鱼 --> IDLE["不更新日志 CMD<br/>写入 data[5]=实际水位、data[6]=实际温度<br/>重算校验后在透传模式发给基站"]
    CMD -- A0..A9 --> SELFCHECK{"当前是否正在自检且命令不是 A8?"}
    SELFCHECK -- 是 --> IGNORE["忽略本次桶体控制命令"]
    SELFCHECK -- 否 --> ACMD{"执行 A 命令"}
    CMD -- B0..BF --> BCMD["桶体仅记录必要状态<br/>透传原业务帧给基站"]
    CMD -- C0..CF --> CCMD["非透传模式执行系统命令<br/>C0 关闭输出并申请复位"]
    CMD -- 其它 --> DROP["忽略"]

    POSGATE["产品门禁：A2/A4/A5/A6 只能在离开基站<br/>并到达指定点位后由 Linux 下发<br/>桶体当前代码本身不识别底盘点位"]
    POSGATE -. 约束 Linux 下发时机 .-> ACMD
    ACMD -- A0 --> A0["关闭全部输出、取消定时<br/>状态 OFF=2"]
    ACMD -- A1 --> A1["关闭全部输出、取消定时<br/>状态 STANDBY=3"]
    ACMD -- A2 --> A2["目标温度=data[6]，限制 35..48℃<br/>启用恒温并打开内循环<br/>状态 RUNNING=4"]
    ACMD -- A3 --> A3["关闭恒温<br/>若 UV 未开则关闭泵阀"]
    ACMD -- A4 --> A4["按摩等级=data[7]<br/>0 停止，非 0 运行"]
    ACMD -- A5 --> A5["UV=data[8]<br/>开 UV 时同步开内循环<br/>关 UV 且未恒温时关闭泵阀"]
    ACMD -- A6 --> A6["只读取 data[9] 设置定时<br/>1/2/3/4/5=10/15/20/25/30 分钟<br/>不改变温度、按摩、UV、泵阀"]
    ACMD -- A7 --> A7["停止全部输出、取消定时<br/>进入待机"]
    ACMD -- A8 --> A8["关闭输出并清错误<br/>进入 SELF_CHECK=1，持续约 5 秒<br/>立即回复 Linux"]
    ACMD -- A9 --> A9["关闭全部输出<br/>进入 LOW_POWER=5"]

    A8 --> A8DONE{"5 秒到期?"}
    A8DONE -- 否 --> LOOP
    A8DONE -- 是 --> A8REPORT["状态回 POWER_ON=0<br/>向 Linux 单独发送自检完成帧"]

    BASEFRAME --> BASEVALID{"基站帧有效?"}
    BASEVALID -- 否 --> SAFETY
    BASEVALID -- 是 --> CACHE["缓存基站 data[16..28]<br/>刷新基站在线时间"]
    CACHE --> BS{"基站主状态 data[23]"}
    DRAINGATE["排水业务门禁：桶体必须已回到并连接基站<br/>收到基站排水状态后才同步排水"]
    DRAINGATE -. 连接条件 .-> DRAIN
    BS -- 0x08/0x0A/0x0C --> DRAIN["桶体泵阀切换为排水模式"]
    BS -- 0x07/0x09 --> CIRC["桶体泵阀切换为内循环模式"]
    BS -- 其它 --> RELEASE["若原状态是排水则关闭<br/>若是基站触发循环且本地无恒温/UV则关闭<br/>本地恒温或 UV 需要循环时继续保持"]

    SAFETY{"安全监控"}
    SAFETY --> WATER{"加热/循环/UV运行且水位 < 1L?"}
    WATER -- 是 --> E1["ERR1 置缺水位 0x01"]
    WATER -- 否 --> SENSOR
    E1 --> SENSOR{"水位或温度传感器异常?"}
    SENSOR -- 是 --> E2["ERR1 置 0x20/0x10"]
    SENSOR -- 否 --> TEMP
    E2 --> TEMP{"温度 >= 55℃ 或加热超时?"}
    TEMP -- 是 --> E3["报温度/加热故障并停止对应输出"]
    TEMP -- 否 --> CURRENT
    E3 --> CURRENT{"按摩 > 2100mA 或水泵 > 3000mA?"}
    CURRENT -- 是 --> E4["停止故障电机/水泵并设置 ERR2"]
    CURRENT -- 否 --> BAT
    E4 --> BAT{"电池有效且 <20.0V 或 >26.0V?"}
    BAT -- 低于 20.0V --> LOW["ERR2 电池位 0x20<br/>关闭全部输出并进入 LOW_POWER"]
    BAT -- 高于 26.0V --> HIGH["ERR2 电池位 0x20"]
    BAT -- 正常或采样为 0 --> TIMER
    LOW --> TIMER{"定时是否到期?"}
    HIGH --> TIMER
    TIMER -- 是 --> TIMEOUT["关闭恒温、按摩、泵阀、UV<br/>清除定时并进入待机"]
    TIMER -- 否 --> MAINLINK
    TIMEOUT --> MAINLINK{"Linux 是否超过 5 秒无有效帧?"}
    MAINLINK -- 是 --> MAINSTOP["关闭全部输出、取消定时<br/>CMD 记为 A7，进入待机"]
    MAINLINK -- 否 --> REPORT
    MAINSTOP --> REPORT{"到 1 秒上报周期?"}
    REPORT -- 否 --> LOOP
    REPORT -- 是 --> BASELINK{"基站是否 3 秒内在线?"}
    BASELINK -- 是 --> MERGE["生成桶体 data[0..15]<br/>合并基站 data[16..28]<br/>data[3] 常规状态帧为 0"]
    BASELINK -- 否 --> ZERO["生成桶体 data[0..15]<br/>基站 data[16..28] 全部为 0"]
    MERGE --> TX["重算 data[29] 校验并发送 Linux"]
    ZERO --> TX
    TX --> LOOP

    IDLE --> LOOP
    IGNORE --> LOOP
    BCMD --> LOOP
    CCMD --> LOOP
    DROP --> LOOP
    A0 --> LOOP
    A1 --> LOOP
    A2 --> LOOP
    A3 --> LOOP
    A4 --> LOOP
    A5 --> LOOP
    A6 --> LOOP
    A7 --> LOOP
    A9 --> LOOP
    DRAIN --> LOOP
    CIRC --> LOOP
    RELEASE --> LOOP
    A8REPORT --> LOOP
```
### 桶体业务位置前置条件

- 恒温、按摩、UV 和足浴定时属于离站服务功能，只能在 Linux 确认桶体离开基站并到达指定点位后下发。
- 排水属于在站功能，必须先返回并物理对接基站，确认桶体-基站通信在线后执行。
- 当前桶体 MCU 没有底盘点位字段，收到 A2/A4 等命令仍会直接执行，因此位置门禁目前必须由 Linux 保证。

### 如何理解桶体命令

- A0、A1、A7、A9 是整体状态命令，会影响多个输出。
- A2/A3 控制恒温；A2 需要内循环，A3 关闭恒温后只有 UV 也关闭时才释放循环。
- A4 控制按摩，等级来自 `data[7]`；A5 控制 UV，UV 工作需要内循环。
- A6 只读取 `data[9]` 设置时间，不改变其它外设。
- A8 自检期间其它 A 类命令被忽略，并保留开始、完成两次即时上报。
- B 类命令原则上由桶体透传给基站执行。

### 泵阀优先级

1. 基站状态 `0x08/0x0A/0x0C`：桶体切到排水。
2. 基站状态 `0x07/0x09`：桶体切到内循环。
3. 离开上述阶段后，如果本地恒温或 UV 仍需要循环，就继续保持。
4. 定时到期、A7、低电压保护或 Linux 超时会关闭全部输出。

### 桶体上报

- `data[0..15]` 根据桶体实时状态生成。
- 基站在线时合并 `data[16..28]`；离线时该区域全为 0。
- 常规帧 `data[3]=0x00`；自检开始和完成是即时回包例外。



### 桶体任务与互斥重点

| 项目 | 当前代码行为 |
|---|---|
| 恒温控制 | 目标温度限制为 35～48℃；低于目标约 0.5℃开始加热，高于目标约 0.3℃停止加热 |
| 水位范围 | 协议水位最大 12L；需要水的功能在水位低于 1L 时产生缺水错误 |
| 泵阀互斥 | 泵阀只有关闭、内循环、排水三种模式，同一时刻不能同时内循环和排水 |
| 基站同步 | `0x07/0x09` 请求桶体内循环；`0x08/0x0A/0x0C` 请求桶体排水 |
| 定时命令 | A6 只设置时间；到期才统一关闭恒温、按摩、泵阀和 UV |
| 自检互斥 | 自检期间除 A8 外的 A 类桶体命令被忽略；自检开始和完成均有即时回包 |
| 常规上报 | 每秒一帧，正常周期帧 `data[3]=0x00`，不持续携带上一条命令 |

## 三、基站业务逻辑流程图

### 这张图讲什么

第三张图是基站的非阻塞状态机。收到 B 类命令后，基站记录当前动作、目标状态、截止时间和电机位置，再由周期任务推进阶段。

- 左上部分是通信入口：`0x00` 更新实时水位和温度，B0～B9 启动业务。
- 中间部分是 B2 自动注水和 B3 自动清洁。
- 下方部分是通信超时、缺液检查、动作计时和状态上报。

```mermaid
flowchart TD
    B0([基站上电]) --> B1["初始化通信、喷淋位置电机和全部输出<br/>进水、加热、排水、喷淋、药液泵、风机关闭"]
    B1 --> B2["主状态 POWER_ON=0x00<br/>进入非阻塞状态机循环"]
    B2 --> RX{"收到桶体 30 字节帧?"}
    RX -- 否 --> PERIOD
    RX -- 是 --> VALID{"帧有效且 data[2]=0x02?"}
    VALID -- 否 --> PERIOD
    VALID -- 是 --> ONLINE["刷新桶体在线时间<br/>每个有效帧处理后立即回复状态"]
    ONLINE --> CMD{"data[3]"}
    CMD -- 0x00 --> RT["更新实际水位=data[5]<br/>更新实际温度=data[6]<br/>标记实时数据有效"]
    CMD -- B0..BF --> BCMD{"基站命令"}
    CMD -- 其它 --> PERIOD

    BCMD -- B0 --> OFF["取消动作，全部输出关闭<br/>喷淋电机回排水位，状态 OFF=0x02"]
    BCMD -- B1 --> STBY["取消动作，全部输出关闭<br/>喷淋电机回排水位，状态 STANDBY=0x03"]
    BCMD -- B2 --> FILL["目标水位=data[16]<br/>目标温度=data[6]<br/>药液泵时长=data[20]/data[21]<br/>状态 AUTO_FILL=0x04"]
    FILL --> FOUT["打开进水<br/>目标温度非 0 时打开基站加热<br/>按设定时长启动药液泵"]
    RT --> ACTIVE{"当前动作"}
    ACTIVE -- 自动注水 --> FCHK{"有效实时水位 >= 目标水位?"}
    FCHK -- 否 --> HEATCHK["实际温度 < 目标温度时加热<br/>否则关闭加热"]
    HEATCHK --> PERIOD
    FCHK -- 是 --> FDONE["关闭进水和加热，标记注水完成<br/>目标温度非 0 → 0x05<br/>目标温度为 0 → 0x06"]

    BCMD -- B3/B4 --> DOCKCHECK{"桶体是否已连接基站?<br/>至少通信链路有效"}
    DOCKCHECK -- 否 --> NODRAIN["拒绝启动排水/清洁<br/>保持输出关闭并等待重新对接"]
    NODRAIN --> PERIOD
    DOCKCHECK -- B3 且已连接 --> D1P["启动自动清洁<br/>喷淋电机先到排水位 0 度"]
    D1P --> MOTOROK{"位置到达且无电机故障?"}
    MOTOROK -- 否，故障 --> ABORT["关闭输出并回待机"]
    MOTOROK -- 是 --> D1["第一次排水：状态 0x0C<br/>基站排水阀 + 桶体同步排水"]
    D1 --> DRAINRULE1
    DRAINRULE1["排水规则：收到有效水位 0L 后继续 100 秒<br/>一直未到 0L 时最多排 10 分钟"]
    DRAINRULE1 --> C1P["喷淋电机转到喷淋位 90 度"]
    C1P --> C1["清洁喷淋：状态 0x07<br/>桶体同步内循环，总时长 data[17] 分钟"]
    C1 --> CP1["前 10 秒清水"]
    CP1 --> CP2["清洁液 5 秒"]
    CP2 --> CP3["等待 5 秒"]
    CP3 --> CP4["再次清洁液 5 秒"]
    CP4 --> CP5["再次等待 5 秒"]
    CP5 --> CP6["剩余时间继续清水喷淋"]
    CP6 --> D2P["喷淋电机回排水位 0 度"]
    D2P --> D2["第二次排水：状态 0x08<br/>基站与桶体同步排水"]
    D2 --> DRAINRULE2["0L 后继续 100 秒<br/>最长 10 分钟"]
    DRAINRULE2 --> C2P["喷淋电机转喷淋位 90 度"]
    C2P --> C2["清水/热水喷淋：状态 0x09<br/>桶体同步内循环，总时长 data[18] 分钟"]
    C2 --> D3P["喷淋电机回排水位 0 度"]
    D3P --> D3["第三次排水：状态 0x0A<br/>基站与桶体同步排水"]
    D3 --> DRAINRULE3["0L 后继续 100 秒<br/>最长 10 分钟"]
    DRAINRULE3 --> DRY["热风烘干：状态 0x0B<br/>关闭水路，打开风机和加热<br/>持续 data[19] 分钟"]
    DRY --> CLEANEND["关闭全部输出<br/>喷淋电机回排水位<br/>进入 STANDBY=0x03"]

    DOCKCHECK -- B4 且已连接 --> SDRAIN["强制排水：状态 0x0C<br/>到排水位后打开排水"]
    SDRAIN --> SDRULE["0L 后继续 100 秒或最长 10 分钟"]
    SDRULE --> CLEANEND
    BCMD -- B5 --> SINGLEC["单独清洁喷淋：状态 0x0D<br/>执行 data[17] 分钟清洁喷淋模式"]
    SINGLEC --> SDRAIN
    BCMD -- B6 --> SINGLEW["单独清水/热水喷淋：状态 0x0E<br/>执行 data[18] 分钟"]
    SINGLEW --> SDRAIN
    BCMD -- B7 --> SINGLEDRY["单独烘干：状态 0x0F<br/>执行 data[19] 分钟"]
    SINGLEDRY --> CLEANEND
    BCMD -- B8 --> SELF["关闭输出并清错误<br/>喷淋电机执行自检定位<br/>状态 SELF_CHECK=0x01"]
    BCMD -- B9 --> STOPC{"当前是否为自动清洁动作?"}
    STOPC -- 是 --> ABORT
    STOPC -- 否 --> PERIOD

    PERIOD{"到 1 秒周期?"}
    PERIOD -- 是 --> REPORT["主动向桶体发送基站状态<br/>data[23]=主状态<br/>data[24]=剩余分钟或状态持续秒数<br/>data[28]=桶体内循环请求"]
    PERIOD -- 否 --> LINK
    REPORT --> LINK{"桶体是否超过 5 秒无有效帧?"}
    LINK -- 否 --> LEVEL
    LINK -- 是 --> NEEDBUCKET{"当前动作依赖桶体?<br/>自动注水或需要桶体循环"}
    NEEDBUCKET -- 是 --> ABORT
    NEEDBUCKET -- 否 --> LEVEL
    LEVEL{"缺液板是否超过 5 秒无数据<br/>或药液/清洁液不足?"}
    LEVEL -- 药液不足 --> MEDOFF["停止对应药液泵并上报错误"]
    LEVEL -- 清洁液不足且正在清洁喷淋 --> ABORT
    LEVEL -- 通信超时 --> LVERR["设置缺液板通信错误"]
    LEVEL -- 正常 --> STATE
    MEDOFF --> STATE
    LVERR --> STATE{"动作截止时间或排水条件是否满足?"}
    STATE -- 否 --> B2
    STATE -- 是 --> NEXT["按当前 action 推进下一阶段<br/>或结束回待机"]
    NEXT --> B2
    PERIOD -->|继续处理| B2
    FDONE --> PERIOD
    OFF --> PERIOD
    STBY --> PERIOD
    SELF --> PERIOD
    ABORT --> CLEANEND
    CLEANEND --> PERIOD
```
### 基站连接前置条件

- 注水、排水和自动清洁都属于在站动作，Linux 必须先确认桶体已与基站物理对接。
- 基站 MCU 当前能确认的是桶体通信在线，不能仅凭协议确认机械结构已经可靠对接；物理对接结果应由 Linux 的底盘/对接检测提供。
- 未确认连接时不得下发 B3、B4、B5、B6 等涉及水路的命令。

### 如何理解 B2 自动注水

- 目标水位取 B2 的 `data[16]`，目标温度取 `data[6]`。
- 药液泵时长取 `data[20]` 和 `data[21]`。
- 实际水位只取 `0x00` 帧的 `data[5]`，实际温度只取 `data[6]`。
- 收到有效实时数据且实际水位达到目标值，才判定注水完成。
- 所以 B2 发出后 Linux 仍要继续发送摸鱼帧。

### 如何理解 B3 自动清洁

1. **第一次排水 `0x0C`**：排残留水，桶体同步排水。
2. **清洁喷淋 `0x07`**：电机到 90°，桶体内循环，执行清水和两次清洁液。
3. **第二次排水 `0x08`**：排出清洁喷淋后的污水。
4. **清水喷淋 `0x09`**：再次冲洗，桶体同步内循环。
5. **第三次排水 `0x0A`**：排出清水喷淋后的水。
6. **热风烘干 `0x0B`**：关闭水路，开启风机和加热，结束后回到 `0x03`。

排水使用电机 0°，喷淋使用 90°；电机未到位或故障时不能推进。

### 排水结束条件

1. 正在排水、已收到有效水位，并检测到 `data[5]=0L`。
2. 检测到 0L 后，当前代码继续排水 100 秒。
3. 始终未检测到 0L 时，最长 10 分钟后结束该排水阶段。

### B4～B9

- B4 强制排水；B5 单独清洁喷淋；B6 单独清水喷淋；B7 单独烘干。
- B8 进入基站自检；B9 只中止自动清洁，不是通用停止命令。


### 基站状态码与桶体联动

| `data[23]` | 基站状态 | 桶体联动 |
|---:|---|---|
| `0x00` | 上电 | 无 |
| `0x01` | 自检 | 无 |
| `0x02` | 关闭 | 释放基站触发的泵阀动作 |
| `0x03` | 待机 | 释放基站触发的泵阀动作 |
| `0x04` | 自动注水 | 无强制泵阀同步 |
| `0x05` | 注水后保温 | 无强制泵阀同步 |
| `0x06` | 注水完成待排水 | 无强制泵阀同步 |
| `0x07` | 自动清洁喷淋 | 桶体内循环 |
| `0x08` | 清洁喷淋后排水 | 桶体排水 |
| `0x09` | 清水喷淋 | 桶体内循环 |
| `0x0A` | 清水喷淋后排水 | 桶体排水 |
| `0x0B` | 热风烘干 | 关闭基站触发的桶体泵阀动作 |
| `0x0C` | 首次/强制/单项后排水 | 桶体排水 |
| `0x0D` | 单独清洁喷淋 | 当前桶体代码不按此状态强制内循环 |
| `0x0E` | 单独清水喷淋 | 当前桶体代码不按此状态强制内循环 |
| `0x0F` | 单独烘干 | 无 |

## 四、当前实现中需要产品确认的边界

1. **新增业务门禁**：离站并到达指定点位后才能下发恒温、按摩等足浴命令；返回且确认物理对接基站后才能排水或清洁。当前两个 MCU 协议没有明确的底盘点位/机械对接字段，该门禁必须由 Linux 实现，或后续扩展协议。

2. 基站当前代码中，检测到 `0L` 后继续排水是 **100 秒**；如果产品要求是 30 秒，需要后续统一修改常量和测试标准。
3. 自动注水当前没有独立的最长注水超时；若水位数据始终小于目标值，进水可能持续，产品应补充超时和水位传感器失效策略。
4. B2 的目标水位若为 `0`，当前启动逻辑仍可能打开进水，应由 Linux 禁止下发或由基站增加参数合法性检查。
5. 基站 B8 自检当前进入 `0x01` 后没有清晰的自动完成计时和结果闭环，产品流程应定义完成条件、超时时间和退出命令。
6. 桶体只对基站 `0x07/0x09` 同步内循环；单独喷淋状态 `0x0D/0x0E` 当前不会触发桶体内循环，需要确认这是产品预期还是遗漏。
7. 桶体低水位当前会设置错误码，但不同外设模块的停机动作并不完全由同一个总开关完成；整机安全需求应明确哪些错误必须立即停止全部输出。

## Mermaid 预览

在 VS Code 中打开本文件后，使用 `Ctrl+Shift+V` 打开 Markdown 预览。Mermaid 插件通常只在 Markdown 预览页渲染，源码编辑页仍会显示代码块。
